/* ============================================================
   GENERAL STATUS EFFECT RENDERING
   Reserved for rendering helpers shared across MULTIPLE status
   effects (e.g. a shared badge container/positioning helper).
   Currently empty — every existing status-effect visual is
   effect-specific and lives in its own *-rendering.js file.
   ============================================================ */
